# =============================================================================
# TraitQuest Docker 環境變數範本
# =============================================================================
# 使用說明：
#   1. 複製此檔案為 .env.docker
#   2. 填入實際數值
#   3. 執行 ./docker-run.sh up
#
# 注意：容器內連接主機服務須使用 host.docker.internal 替代 localhost
# =============================================================================

# === 服務設定 ===
HOST_PORT=7273

# === 資料庫 ===
DATABASE_URL=postgresql+asyncpg://user:password@host.docker.internal:5432/traitquest

# === Redis ===
REDIS_HOST=host.docker.internal
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# === AI 模型與 API 設定 ===
LITELLM_URL=http://host.docker.internal:4000
LLM_MODEL=github_copilot/gpt-4o
GITHUB_COPILOT_TOKEN=your-github-token
GITHUB_COPILOT_HEADERS={"editor-version": "vscode/1.85.1", "editor-plugin-version": "copilot/1.155.0", "Copilot-Integration-Id": "vscode-chat", "user-agent": "GithubCopilot/1.155.0"}

# === 應用程式安全 ===
SECRET_KEY=your-super-secret-key-change-me
APP_ENV=production
LOG_FILE_PATH=/app/logs/traitquest.log

# === CORS 設定 ===
# 注意：正式環境請改為實際網域
CORS_ORIGINS=["http://localhost:3000", "http://localhost:8000"]

# === Google OAuth ===
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# === 前端設定（建置時注入）===
# 這些變數在 docker build 時使用 --build-arg 傳入
# VITE_API_BASE_URL=/v1
# VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SENDER_EMAIL=your-sender-email
SMTP_PASSWORD=your-smtp-password
